[package]
name = "sampler-core"
version = "0.1.0"
edition = "2024"

[lib]
name = "sampler_core"
path = "src/lib.rs"

[dependencies]
dasp_sample =  { version = "0.11.0" , features = []}
dasp_signal =  { version = "0.11.0" , features = ["all-no-std"]}
